<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{{ title or "Bridget McTiernan" }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="{{ '/style.css' | url }}" />
  </head>
  <body>
    <div class="container">
      <header>
        <nav>
          <div class="logo">
            <a href="{{ '/' | url }}">Bridget McTiernan</a>
          </div>

          <button class="menu-toggle" aria-label="Toggle navigation">
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
          </button>

          <div class="nav-links">
            <a
              href="{{ '/projects/' | url }}"
              class="{% if '/projects/' in page.url %}active{% endif %}"
              >Projects</a
            >
            <a
              href="{{ '/about/' | url }}"
              class="{% if '/about/' in page.url %}active{% endif %}"
              >About</a
            >
            <a
              href="{{ '/contact/' | url }}"
              class="{% if '/contact/' in page.url %}active{% endif %}"
              >Contact</a
            >
            <a
              href="https://www.canva.com/design/DAG75SscInw/bwT4fsU8PeybFpw7AtUbdg/view?utm_content=DAG75SscInw&utm_campaign=designshare&utm_medium=link2&utm_source=uniquelinks&utlId=hc11888f864"
              target="_blank"
              rel="noopener"
              >Resume</a
            >
          </div>
        </nav>
      </header>

      <main>{{ content | safe }}</main>

      <footer>&copy; 2024 Bridget McTiernan</footer>
    </div>

    <script>
      document
        .querySelector('.menu-toggle')
        .addEventListener('click', function () {
          document.querySelector('.nav-links').classList.toggle('open');
        });
    </script>
  </body>
</html>
